import { CloudArrowDownIcon } from "@heroicons/react/24/outline";

const Spinner = () => {
   return (
      <CloudArrowDownIcon className="mx-auto h-6 w-6 text-gray-500 animate-bounce" />
   );
};

export default Spinner;
